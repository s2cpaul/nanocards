# nAnoCards

This is a code bundle for nAnoCards. The original project is available at https://www.figma.com/design/Vt6wNr2TUaayrKVuPdYyLK/nAnoCards.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
